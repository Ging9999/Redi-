"""Redi — cross-machine coordination for Claude Code agents.

A coordination layer so agents on different machines know when another agent is
already editing a file — and what it's doing — and can adapt instead of
colliding at merge time.
"""

__version__ = "1.2.0"
